<ul>
  <% @categories.each do |category| %>
    <li>
      <h1><%= category.name %></h1>
      <ul>
        <% category.jobs.each do  |job| %>
          <li><%= link_to job.title, job_path(job)  %></li>
        <% end  %>
      </ul>
    </li>
  <% end %>
</ul>