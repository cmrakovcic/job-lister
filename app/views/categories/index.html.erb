<ul>
  <% @categories.each do |category| %>
    <li>
      <%= category.name %>
      <ol>
        <% category.jobs.each do  |job| %>
          <li><%= link_to job.title, job_path(job)  %></li>
        <% end  %>
      </ol>
    </li>
  <% end %>
</ul>